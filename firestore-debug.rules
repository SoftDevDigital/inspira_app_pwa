rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // REGLAS TEMPORALES PERMISIVAS PARA DEBUG
    // ⚠️ NO USAR EN PRODUCCIÓN - Solo para diagnosticar problemas
    
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
